# AI Sundays · https://www.aisundays.org
User-agent: *
Allow: /

# The API is data, not pages. It has no useful content for a crawler and every
# GET is edge-cached, so crawling it only burns cache.
Disallow: /api/

Sitemap: https://www.aisundays.org/sitemap.xml
