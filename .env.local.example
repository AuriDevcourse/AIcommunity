# Local dev only. Copy to .env.local and adjust as needed.
# Enables the floating Feedback button (requires server.js or vite dev middleware).
# On Vercel: leave unset — the API endpoint isn't available on serverless.
VITE_FEEDBACK_ENABLED=true
